---
title: 提示
描述: 附加资源和技巧
---

# 额外

附加资源和技巧，以增强您的开发体验。

## 有用的链接

### 文档

- [Next.js 文档](https://nextjs.org/docs)
- [React 文档](https://react.dev)
- [TypeScript 文档](https://www.typescriptlang.org/docs)
- [Tailwind CSS 文档](https://tailwindcss.com/docs)
- [Prisma 文档](https://www.prisma.io/docs)

### 工具

- [shadcn/ui 组件](https://ui.shadcn.com)
- [Radix UI 基本组件](https://www.radix-ui.com)
- [Lucide 图标](https://lucide.dev)
- [Vercel](https://vercel.com)

## 开发技巧

### VS Code 扩展

推荐的扩展：

- **ES7+ React/Redux/React-Native 片段**: 代码片段
- **Tailwind CSS IntelliSense**: Tailwind 的自动补全
- **Prisma**: Prisma 模式支持
- **ESLint**: 代码检查
- **Prettier**: 代码格式化
- **GitLens**: Git 集成

### 代码片段

添加到 `.vscode/settings.json`：

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "typescript.preferences.importModuleSpecifier": "relative"
}
```

### Git Hooks

使用 Husky 进行 Git hooks：

```bash
pnpm add -D husky lint-staged
npx husky init
```

`.husky/pre-commit`：

```bash
#!/bin/sh
pnpm lint-staged
```

`package.json`：

```json
{
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": ["eslint --fix", "prettier --write"],
    "*.{json,md,mdx}": ["prettier --write"]
  }
}
```

## 性能优化

### 包分析

分析您的包大小：

```bash
pnpm add -D @next/bundle-analyzer
```

`next.config.ts`：

```typescript
import bundleAnalyzer from '@next/bundle-analyzer';

const withBundleAnalyzer = bundleAnalyzer({
  enabled: process.env.ANALYZE === 'true',
});

export default withBundleAnalyzer(nextConfig);
```

运行分析：

```bash
ANALYZE=true pnpm run build
```

### 懒加载

```tsx
import dynamic from 'next/dynamic';

const HeavyComponent = dynamic(() => import('./HeavyComponent'), {
  loading: () => <p>Loading...</p>,
  ssr: false,
});
```

### 图片优化

```tsx
import Image from 'next/image';

<Image
  src="/image.jpg"
  alt="Description"
  width={500}
  height={300}
  quality={85}
  placeholder="blur"
  blurDataURL="data:image/..."
/>
```

## 测试

### Vitest 设置

```bash
pnpm add -D vitest @vitejs/plugin-react
```

`vitest.config.ts`：

```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
  },
});
```

### 示例测试

```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { Button } from '@/components/ui/button';

describe('Button', () => {
  it('renders correctly', () => {
    render(<Button>Click me</Button>);
    expect(screen.getByText('Click me')).toBeInTheDocument();
  });
});
```

## 国际化（i18n）

启动器已包含 `next-intl` 的国际化。

### 添加新语言

1. 创建消息文件：

```json
// i18n/messages/es.json
{
  "common": {
    "welcome": "Bienvenido"
  }
}
```

2. 更新路由：

```typescript
// i18n/routing.ts
export const routing = {
  locales: ['en', 'zh', 'es'],
  defaultLocale: 'en',
};
```

### 使用翻译

```tsx
import { useTranslations } from 'next-intl';

export function Component() {
  const t = useTranslations('common');

  return <h1>{t('welcome')}</h1>;
}
```

## API 文档

使用 OpenAPI/Swagger 进行 API 文档：

```bash
pnpm add swagger-ui-react
```

创建 API 文档页面：

```tsx
// app/[locale]/api-docs/page.tsx
import SwaggerUI from 'swagger-ui-react';
import 'swagger-ui-react/swagger-ui.css';

export default function ApiDocs() {
  return <SwaggerUI url="/api/openapi.json" />;
}
```

## 数据库 GUI

### Prisma Studio

```bash
pnpm prisma studio
```

在 `http://localhost:5555` 打开

### 替代工具

- [TablePlus](https://tableplus.com)
- [DBeaver](https://dbeaver.io)
- [pgAdmin](https://www.pgadmin.org)

## 调试

### Next.js 调试模式

```json
// .vscode/launch.json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Next.js: debug server-side",
      "type": "node-terminal",
      "request": "launch",
      "command": "pnpm run dev"
    },
    {
      "name": "Next.js: debug client-side",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:3000"
    }
  ]
}
```

### React 开发者工具

安装浏览器扩展：
- [Chrome](https://chrome.google.com/webstore/detail/react-developer-tools/fmkadmapgofadopljbjfkapdkoienihi)
- [Firefox](https://addons.mozilla.org/en-US/firefox/addon/react-devtools/)

## 代码质量

### 类型覆盖

检查 TypeScript 覆盖：

```bash
pnpm add -D type-coverage
```

```json
{
  "scripts": {
    "type-coverage": "type-coverage"
  }
}
```

### 代码复杂性

使用 ESLint 复杂性规则：

```json
{
  "rules": {
    "complexity": ["error", 10]
  }
}
```

## 有用的脚本

添加到 `package.json`：

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "format": "prettier --write .",
    "type-check": "tsc --noEmit",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:migrate": "prisma migrate dev",
    "db:seed": "tsx prisma/seed.ts",
    "clean": "rm -rf .next node_modules",
    "reinstall": "pnpm run clean && pnpm install"
  }
}
```

## 环境设置

### 本地 HTTPS

本地测试 OAuth：

```bash
pnpm add -D local-ssl-proxy
```

```json
{
  "scripts": {
    "dev:https": "local-ssl-proxy --source 3001 --target 3000"
  }
}
```

### 多环境

```
.env.local          # 本地开发
.env.development    # 开发
.env.staging        # 预发布
.env.production     # 生产
```

## 资源

### 学习

- [Next.js 学习](https://nextjs.org/learn)
- [React 教程](https://react.dev/learn)
- [TypeScript 手册](https://www.typescriptlang.org/docs/handbook/intro.html)
- [Tailwind CSS 教程](https://tailwindcss.com/docs/utility-first)

### 社区

- [Next.js Discord](https://nextjs.org/discord)
- [React Discord](https://react.dev/community)
- [GitHub 讨论](https://github.com/vercel/next.js/discussions)

### 博客

- [Vercel 博客](https://vercel.com/blog)
- [Next.js 博客](https://nextjs.org/blog)
- [Lee Robinson](https://leerob.io)

## 支持

如果您需要帮助：

1. 查看 [文档](/docs)
2. 搜索 [GitHub Issues](https://github.com/yourusername/yourrepo/issues)
3. 在 [Discussions](https://github.com/yourusername/yourrepo/discussions) 中询问
4. 联系 support@yourapp.com

## 贡献

我们欢迎贡献！ 请参阅我们的 [贡献指南](https://github.com/yourusername/yourrepo/blob/main/CONTRIBUTING.md)。

## 许可证

本项目采用 MIT 许可 - 参见 [许可证](https://github.com/yourusername/yourrepo/blob/main/LICENSE)。
